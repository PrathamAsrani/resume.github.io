import Typed from 'typed.js';

let options = {
    strings: ["Full Stack Web Development", "Cloud Computing: DevOps", "Data Management"],
    typeSpeed: 100,
    backSpeed: 100,
    loop: true
};

let typed = new Typed(".auto-type", options);